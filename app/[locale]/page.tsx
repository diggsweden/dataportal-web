/**
 * TEMPORARY placeholder to activate the `[locale]` dynamic segment so
 * the chrome (Header / Footer / Sidebar / Breadcrumbs) renders on `/sv`
 * and `/en` during manual chrome verification. Replaced by the real
 * start page port in the next Phase 4 step.
 */
export default function LocaleIndexStub() {
  return <div>Hello World</div>;
}
